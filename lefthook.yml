pre-commit:
  parallel: true
  commands:
    lint:
      run: npx biome check --write --unsafe --staged --no-errors-on-unmatched && git add -u
    typecheck:
      run: npm run typecheck
    build:
      run: npm run build
    test:
      run: npm run test:ci
